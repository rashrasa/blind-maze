{
  "name": "@blind-maze/random",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "pkg/random.js",
  "types": "pkg/random.d.ts",
  "files": [
    "pkg/**"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --release",
    "test-random-distribution": "Unimplemented: @blind-maze/random is being moved to Rust"
  },
  "devDependencies": {
  }
}
